dist/
.astro/
# /rate-frames is regenerated by scripts/sync-data.mjs from
# benches/reports/frames/ on every dev/build. The source frames are large
# auto-rendered PNGs and are not committed; the copy in public/ stays
# uncommitted for the same reason.
public/rate-frames/
src/data/rate-frames.json
node_modules/
.vercel/
.env
.env.*
!.env.example
.vercel
.env*.local
