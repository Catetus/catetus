//! Placeholder until the in-flight usdc-bit-exact agent lands the real
//! round-trip suite. Compiles to a single passing test so cargo's workspace
//! build stays green.

#[test]
fn placeholder() {
    // Real coverage lives in tests/roundtrip.rs; this file is referenced from
    // Cargo.toml [[test]] and must exist on disk.
}
