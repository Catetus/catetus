{
  "name": "@catetus/website",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "description": "catetus.com landing page. Static SPA (Vite + TypeScript). Stealth — do not deploy without explicit approval.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.4.10"
  }
}
