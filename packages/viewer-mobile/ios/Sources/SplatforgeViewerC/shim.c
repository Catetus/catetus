/* Empty translation unit — symbols come from the Rust staticlib that the
 * XCFramework script links in. Having one .c here keeps SwiftPM happy when
 * SplatforgeViewerC is consumed before the XCFramework is built. */
#include "splatforge_viewer_mobile.h"
